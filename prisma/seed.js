// Privileged accounts must be provisioned through the setup/reset flow or deployment secrets.
// This seed deliberately never contains credentials and is safe to run in any environment.
console.log("No privileged users seeded. Use the documented secure account setup procedure.");
